#include <stdio.h>
/**
 * main - prints first 50 fibonacci numbers
 *
 * Return: 0
 */

int main(void)
{
	int i;
	int x;
	int y;

	for (i = 0, x = 1, y; a <= 50; y = i + x, i = x, x = y)
	printf("%d ,", i)
	return (0);
}
