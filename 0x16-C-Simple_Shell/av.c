#include <stdio.h>

/**
 * main - function
 * @ac: number of items in av
 * @av: array of strings
 */

int main(int ac, char **av)
{
	int i;
	int n;

	for (i = 0; i < av[n]; i++)
	{
		(void)*ac;
		printf("%d\n", av[n]);
	}
	return (0);
}
