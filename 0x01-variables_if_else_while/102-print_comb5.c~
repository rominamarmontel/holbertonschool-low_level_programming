#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/* more headers goes there */

/**
 *main - main
 *Return:return
 */

int main(void)
{
	int x, y;

	for (x = '0'; x < '99'; x++)
	{
		for (y = '0'; y <= '99'; y++)
		{
			if (x < y)
			{
				putchar(x);
				putchar(' ');
				putchar(y);
				if (x != '99' && y == '99')
				{
					putchar(',');
					putchar(' ');
				}
			}
		}
	}
	putchar('\n');
	return (0);
}
