#include "main.h"
/**
 *prints the numbers, from 0 to 9
 *
 *4. I believe in numbers and signs
 *@i:number
 */

void print_most_numbers(void);
{
int i;

for (i = 0; i <= 9; i++)
{
if(i != 2 && i != 4)
_putchar(i + '0');
}
_putchar('\n');
}
