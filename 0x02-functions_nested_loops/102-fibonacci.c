#include <stdio.h>
/**
 * main - prints first 50 fibonacci numbers
 *
 * Return: 0
 */

int main(void)
{
	int i;
	int x;

	if (i == 1 || i == 2)
		x = (i - 1) + (i - 2);
	printf("%d ,", x)
	return (0);
}
